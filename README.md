# User Manager CLI (Python)

A simple command-line application to manage users using Python.

## Features

- Add users
- Edit users
- Delete users
- Show statistics (average age, oldest, youngest)
- Persistent storage using JSON
- Object-Oriented Design

## How to run

```bash
python main.py
